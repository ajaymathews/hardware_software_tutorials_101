#include <stdio.h>
#include "arithmetic.h"

int sum(int x, int y){
    printf("sum operation is performed\n");
    return x+y;
}
int sub(int x, int y){
    printf("sum operation is performed\n");
    return x-y;
}
int div(int x, int y){
    printf("sum operation is performed\n");
    return x/y;
}
int mul(int x, int y){
    printf("sum operation is performed\n");
    return x*y;
}