#include<stdio.h>
#include<stdio.h>
void main()
{
/*  int *ptr;
    printf("pointer:%d\n",ptr); /* the pointer will always have a garbage value 
                                    and this will point to that adress initially*/
    int *ptr=NULL;
    printf("pointer:%d\n",ptr);/*the adress will be zero so the value in    
                                location*/
}