/*
    #ifndef STDIO_H
    #define STDIO_H
    #include<stdio.h>
    #endif            */

/* OR
#pragma once
this makes suer that the library included only once*/


int sum(int x, int y);
int sub(int x, int y);
int div(int x, int y);
int mul(int x, int y);